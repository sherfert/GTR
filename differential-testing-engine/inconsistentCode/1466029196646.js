/* 
* Randomly generated JS program on Thu Jun 16 2016 00:19:56 GMT+0200 (CEST)
* Corpus: corpusForTestingJS/js_dataset/data selected top 100000 valid file/s 
* Random seed: true
* Seed: 58682341296273
*/

function unbuild_(input) {
    if (unbuild_(input) && !input(SVGElementWrappingImplementation) && this.input.input.SVGElementWrappingImplementation.x === 3 && SVGElementWrappingImplementation() > false && !unbuild_.unbuild_.getAttribute) {
        if (this.length === 'px') {
            computeFrame(unbuild_);
        }
    } else {
        if (input && !((unbuild_ || emptyObject) && !SVGElementWrappingImplementation.add.relations)) {
            emptyObject(unbuild_);
        }
    }
    assertEquals(a, vars, plotX).assertEquals.plotX = 1;
    SVGElementWrappingImplementation.className.namespaceName = input;
    return a(thisObj, element);
}
alert();
function input(unbuild_, a, b, options, request, name) {
    getAttribute.undefined.a = done();
    this.a = unbuild_;
    length.name.findByCid(false);
    var unbuild_ = require(alert);
    func(expect(options));
    if (describe(request, name, getAttribute))
        input.options.deferId(this, 'visible');
    var input = this;
}
emptyObject.token.prototype({});
input = this.Element.set.string;
a.callee.toLowerCase = vars.$apply.getuid;